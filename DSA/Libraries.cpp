#include <bits/stdc++.h>
using namespace std;
 void explainVector(){
    vector<int> v;

    v.push_back(1); // adds 1 to the vector
    v.emplace_back(2) // adds 2 to vector and is faster than push back
    
 }